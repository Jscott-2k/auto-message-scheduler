{
  "name": "auto-message-scheduler",
  "version": "1.0.0",
  "description": "Runs scheduled Discord webhook messages based on user-defined times.",
  "main": "scheduler.js",
  "scripts": {
    "start": "node scheduler.js",
    "generate-scheduler": "node generate-scheduler-yml.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "discord.js": "^14.14.1"
  }
}